# Quarto 中介產物（不需 commit）
_freeze/
.quarto/

# R / RStudio session 檔
.Rproj.user/
.Rhistory
.RData
.Ruserdata
*.Rproj

# OS
.DS_Store
Thumbs.db

# Quarto render output — 不 commit；PDF 用 GitHub Release asset 散布，HTML 用本地 / Posit Cloud
_book/

# Quarto HTML 相依資產（render 時自動產生：bootstrap / clipboard / search 等）
site_libs/
/*_files/

# Standalone scripts（01–05）的輸出圖表 / HTML — generated，不 commit
output/

/.quarto/
